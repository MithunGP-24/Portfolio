

const options = {
    strings: ["Developer", "Designer", "Programmer"], // List of words you want to type
    typeSpeed: 100,
    backSpeed: 50,
    backDelay: 1000,
    startDelay: 500,
    loop: true
};
new Typed(".text", options);
